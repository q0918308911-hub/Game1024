import { _decorator, } from 'cc';
import { UniReelViewExample } from './UniReelViewTempo';
import { UniSlotMachine } from '../../../../ReelTemplate/v3';
const { ccclass, property } = _decorator;

@ccclass('UniSlotMachineTempo')
export class UniSlotMachineTempo extends UniSlotMachine<UniReelViewExample> {
    start() {

    }

    update(deltaTime: number) {

    }
}